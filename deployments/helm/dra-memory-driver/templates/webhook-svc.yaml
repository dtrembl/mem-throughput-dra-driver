{{- if .Values.webhook.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dra-memory-driver.fullname" . }}-webhook
  namespace: {{ include "dra-memory-driver.namespace" . }}
  labels:
    {{- include "dra-memory-driver.labels" . | nindent 4 }}
    app.kubernetes.io/component: webhook
spec:
  selector:
    {{- include "dra-memory-driver.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: webhook
  ports:
  - name: webhook
    protocol: TCP
    port: {{ .Values.webhook.servicePort }}
    targetPort: webhook
{{- end }}
