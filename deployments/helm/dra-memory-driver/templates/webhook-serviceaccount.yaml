{{- if .Values.webhook.enabled }}
{{- if .Values.webhook.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dra-memory-driver.webhookServiceAccountName" . }}
  namespace: {{ include "dra-memory-driver.namespace" . }}
  labels:
    {{- include "dra-memory-driver.labels" . | nindent 4 }}
  {{- with .Values.webhook.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
